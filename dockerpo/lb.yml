apiVersion: v1
kind:  Service
metadata:
      name: loadbalancer
spec:
    selector:
        app:  apache2app
    type: LoadBalancer
    ports:
      - port: 80
        targetPort: 80
        name: http